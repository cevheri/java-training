import React from 'react';
import { RouteComponentProps } from 'react-router-dom';

const Newpage = (props: RouteComponentProps<{ url: string }>) => {
  const { match } = props;
  return <div>asdsadas</div>;
};

export default Newpage;
